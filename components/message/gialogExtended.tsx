import { MessageExtended } from "../../types/message";

interface DialogExtendedProps {
    dialogId: string;
}

const DialogExtended = ({ dialogId }: DialogExtendedProps) => {
    return <></>;
};

export default DialogExtended;
